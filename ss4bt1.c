#include <stdio.h>
#include <stdlib.h>

int main() {
	int a,b,c;
	printf ("\n nhap so: ");
	scanf("%d",&a);
	printf ("\n nhap so: ");
	scanf("%d",&b);
	printf ("\n nhap so: ");
	scanf("%d",&c);
	
	printf("tong ba so la: %d",a+b+c);
	return 0;
}
